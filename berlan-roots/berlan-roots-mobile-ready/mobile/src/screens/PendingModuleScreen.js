import React from 'react';import {Text} from 'react-native';import {Screen,Header,Card,Badge} from '../components/ui';import {C} from '../theme';
export default function PendingModuleScreen({route}){const titulo=route.params?.titulo||'Módulo';return <Screen><Header title={titulo} subtitle="Tela preparada para a próxima camada"/><Card><Badge tone="gold">Backend pendente</Badge><Text style={{fontSize:19,fontWeight:'800',color:C.pine,marginTop:14}}>Interface reservada no aplicativo</Text><Text style={{color:C.soft,lineHeight:20,marginTop:8}}>O schema atual do Supabase não possui tabelas específicas para este módulo. A navegação já está pronta, mas dados e ações só devem ser ativados depois que a estrutura de banco e as regras forem definidas e testadas.</Text></Card></Screen>}
