import React,{useState} from 'react';
import {Text,View} from 'react-native';
import {Screen,Field,Btn,Card} from '../components/ui'; import {C} from '../theme'; import {supabase} from '../lib/supabase';
export default function AuthScreen(){const[mode,setMode]=useState('login');const[email,setEmail]=useState('');const[senha,setSenha]=useState('');const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');async function submit(){setBusy(true);setMsg('');const {error}=mode==='login'?await supabase.auth.signInWithPassword({email:email.trim(),password:senha}):await supabase.auth.signUp({email:email.trim(),password:senha});setBusy(false);if(error)setMsg(error.message);else if(mode==='cadastro')setMsg('Cadastro realizado. Verifique seu e-mail, se a confirmação estiver ativa.');}
return <Screen><View style={{paddingTop:54,paddingBottom:26}}><Text style={{fontSize:34,fontWeight:'900',color:C.pine}}>ROOTS</Text><Text style={{color:C.soft,marginTop:5}}>Cobranças e empréstimos na palma da mão.</Text></View><Card><Field label="E-mail" value={email} onChangeText={setEmail} keyboardType="email-address" autoCapitalize="none"/><Field label="Senha" value={senha} onChangeText={setSenha} secureTextEntry/><Btn title={busy?'Aguarde...':mode==='login'?'Entrar':'Criar conta'} onPress={submit} disabled={busy}/>{msg?<Text style={{color:C.red,marginTop:12}}>{msg}</Text>:null}<Btn kind="secondary" title={mode==='login'?'Criar uma conta':'Já tenho conta'} onPress={()=>setMode(mode==='login'?'cadastro':'login')}/></Card></Screen>}
